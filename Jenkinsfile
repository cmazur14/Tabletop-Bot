pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
                sh 'echo "Hey, we updated a thing!"'
            }
        }
    }
}